#pragma once
#include <string>

using namespace std;

enum PATTERNSHIP
{
    PATTERN = 13    //Continue indexing from Relationship.h
};

enum PatternType
{
    ASSIGN_PATTERN = 1,
    WHILE_PATTERN,
    IF_PATTERN
};
